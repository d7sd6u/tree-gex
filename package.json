{
  "name": "tree-gex",
  "version": "0.0.2",
  "description": "Match tree structures using regex-inspired tree matchers",
  "main": "dist/index.js",
  "type": "module",
  "scripts": {
    "test": "vitest --run",
    "test:dev": "vitest",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "build": "rm -rf dist && pnpm tsup  ./src --dts --format esm --no-splitting",
    "prepack": "pnpm build"
  },
  "keywords": [],
  "author": "d7sd6u",
  "license": "LGPL-3.0-or-later",
  "devDependencies": {
    "@trivago/prettier-plugin-sort-imports": "^5.2.2",
    "@tsconfig/strictest": "^2.0.5",
    "@vitest/coverage-v8": "3.2.3",
    "esbuild": "^0.25.5",
    "prettier": "^3.5.3",
    "tsup": "^8.5.0",
    "typescript": "^5.8.3",
    "vite": "^6.3.5",
    "vitest": "^3.2.3"
  },
  "optionalDependencies": {
    "@types/node": "^24.0.1",
    "graphql": "^16.11.0",
    "tsx": "^4.20.3"
  }
}
